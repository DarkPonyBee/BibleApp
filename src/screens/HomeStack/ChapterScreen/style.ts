import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  screenContainer: {
    flex: 1,
  },
  backBtn: {
    marginTop: 8,
    marginLeft: 12,
  },
  backTitle: {
    fontSize: 16,
    fontWeight: '800',
  },
  container: {
    paddingHorizontal: 12,
  },
  reference: {
    fontSize: 24,
  },
  description: {
    fontSize: 14,
    overflow: 'hidden',
    marginBottom: 8,
  },
});
